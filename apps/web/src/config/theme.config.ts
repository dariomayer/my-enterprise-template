export const themeConfig = {
  // Add centralized configuration options that might be used by JS
  // Most styling should be derived from globals.css
  appName: 'Enterprise Platform',
  defaultTheme: 'system', // 'light' | 'dark' | 'system'
};
